{# purpose of this template: tracks admin delete confirmation view #}
{% extends 'MUSoundModule::adminBase.html.twig' %}
{% block title __('Delete track') %}
{% block admin_page_icon 'trash-o' %}
{% block content %}
    <div class="musoundmodule-track musoundmodule-delete">
        <p class="alert alert-warning">{{ __f('Do you really want to delete this track: "%name%" ?', {'%name%': track|musoundmodule_formattedTitle}) }}</p>

        {% form_theme deleteForm with [
            '@MUSoundModule/Form/bootstrap_3.html.twig',
            'ZikulaFormExtensionBundle:Form:form_div_layout.html.twig'
        ] %}
        {{ form_start(deleteForm) }}
        {{ form_errors(deleteForm) }}

        {% if formHookTemplates|length > 0 %}
            <fieldset>
                {% for hookTemplate in formHookTemplates %}
                    {{ include(hookTemplate.0, hookTemplate.1, ignore_missing = true) }}
                {% endfor %}
            </fieldset>
        {% endif %}
        <fieldset>
            <legend>{{ __('Confirmation prompt') }}</legend>
            <div class="form-group">
                <div class="col-sm-offset-3 col-sm-9">
                    {{ form_widget(deleteForm.delete) }}
                    {{ form_widget(deleteForm.cancel) }}
                </div>
            </div>
        </fieldset>
        {{ form_end(deleteForm) }}

        {{ block('display_hooks') }}
    </div>
{% endblock %}
{% block display_hooks %}
    {% set hooks = notifyDisplayHooks(eventName='musoundmodule.ui_hooks.tracks.form_delete', id=track.getKey()) %}
    {% if hooks is iterable and hooks|length > 0 %}
        {% for providerArea, hook in hooks %}
            <fieldset>
                {# <legend>{{ hookName }}</legend> #}
                {{ hook }}
            </fieldset>
        {% endfor %}
    {% endif %}
{% endblock %}
