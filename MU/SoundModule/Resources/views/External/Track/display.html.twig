{# Purpose of this template: Display one certain track within an external context #}
<div id="track{{ track.getKey() }}" class="musoundmodule-external-track">
{% if displayMode == 'link' %}
    <p class="musoundmodule-external-link">
    <a href="{{ path('musoundmodule_track_display', {'id': track.getId()}) }}" title="{{ track|musoundmodule_formattedTitle|e('html_attr') }}">
    {{ track|musoundmodule_formattedTitle|notifyFilters('sound.filter_hooks.tracks.filter')|safeHtml }}
    </a>
    </p>
{% endif %}
{% if hasPermission('MUSoundModule::', '::', 'ACCESS_EDIT') %}
    {# for normal users without edit permission show only the actual file per default #}
    {% if displayMode == 'embed' %}
        <p class="musoundmodule-external-title">
            <strong>{{ track|musoundmodule_formattedTitle|notifyFilters('sound.filter_hooks.tracks.filter')|safeHtml }}</strong>
        </p>
    {% endif %}
{% endif %}

{% if displayMode == 'link' %}
{% elseif displayMode == 'embed' %}
    <div class="musoundmodule-external-snippet">
        &nbsp;
    </div>

    {# you can distinguish the context like this: #}
    {# % if source == 'contentType' %}
        ...
    {% elseif source == 'scribite' %}
        ...
    {% endif % #}

    {# you can enable more details about the item: #}
    {#
        <p class="musoundmodule-external-description">
            {% if track.description is not empty %}{{ track.description }}<br />{% endif %}
        </p>
    #}
{% endif %}
</div>
