<?php
/**
 * Sound.
 *
 * @copyright Michael Ueberschaer (MU)
 * @license http://www.gnu.org/licenses/lgpl.html GNU Lesser General Public License
 * @author Michael Ueberschaer <info@homepages-mit-zikula.de>.
 * @link https://homepages-mit-zikula.de
 * @link http://zikula.org
 * @version Generated by ModuleStudio (https://modulestudio.de).
 */

namespace MU\SoundModule\Container\Base;

use Symfony\Component\Routing\RouterInterface;
use Zikula\Common\Translator\TranslatorInterface;
use Zikula\Common\Translator\TranslatorTrait;
use Zikula\Core\Doctrine\EntityAccess;
use Zikula\Core\LinkContainer\LinkContainerInterface;
use Zikula\ExtensionsModule\Api\ApiInterface\VariableApiInterface;
use Zikula\PermissionsModule\Api\ApiInterface\PermissionApiInterface;
use MU\SoundModule\Helper\ControllerHelper;

/**
 * This is the link container service implementation class.
 */
abstract class AbstractLinkContainer implements LinkContainerInterface
{
    use TranslatorTrait;

    /**
     * @var RouterInterface
     */
    protected $router;

    /**
     * @var PermissionApiInterface
     */
    protected $permissionApi;

    /**
     * @var VariableApiInterface
     */
    protected $variableApi;

    /**
     * @var ControllerHelper
     */
    protected $controllerHelper;

    /**
     * LinkContainer constructor.
     *
     * @param TranslatorInterface    $translator       Translator service instance
     * @param Routerinterface        $router           Router service instance
     * @param PermissionApiInterface $permissionApi    PermissionApi service instance
     * @param VariableApiInterface   $variableApi      VariableApi service instance
     * @param ControllerHelper       $controllerHelper ControllerHelper service instance
     */
    public function __construct(
        TranslatorInterface $translator,
        RouterInterface $router,
        PermissionApiInterface $permissionApi,
        VariableApiInterface $variableApi,
        ControllerHelper $controllerHelper
    ) {
        $this->setTranslator($translator);
        $this->router = $router;
        $this->permissionApi = $permissionApi;
        $this->variableApi = $variableApi;
        $this->controllerHelper = $controllerHelper;
    }

    /**
     * Sets the translator.
     *
     * @param TranslatorInterface $translator Translator service instance
     */
    public function setTranslator(/*TranslatorInterface */$translator)
    {
        $this->translator = $translator;
    }

    /**
     * Returns available header links.
     *
     * @param string $type The type to collect links for
     *
     * @return array Array of header links
     */
    public function getLinks($type = LinkContainerInterface::TYPE_ADMIN)
    {
        $contextArgs = ['api' => 'linkContainer', 'action' => 'getLinks'];
        $allowedObjectTypes = $this->controllerHelper->getObjectTypes('api', $contextArgs);

        $permLevel = LinkContainerInterface::TYPE_ADMIN == $type ? ACCESS_ADMIN : ACCESS_READ;

        // Create an array of links to return
        $links = [];

        if (LinkContainerInterface::TYPE_ACCOUNT == $type) {
            if (!$this->permissionApi->hasPermission($this->getBundleName() . '::', '::', ACCESS_OVERVIEW)) {
                return $links;
            }

            if (true === $this->variableApi->get('MUSoundModule', 'linkOwnAlbumsOnAccountPage', true)) {
                $objectType = 'album';
                if ($this->permissionApi->hasPermission($this->getBundleName() . ':' . ucfirst($objectType) . ':', '::', ACCESS_READ)) {
                    $links[] = [
                        'url' => $this->router->generate('musoundmodule_' . strtolower($objectType) . '_view', ['own' => 1]),
                        'text' => $this->__('My albums', 'musoundmodule'),
                        'icon' => 'list-alt'
                    ];
                }
            }

            if (true === $this->variableApi->get('MUSoundModule', 'linkOwnTracksOnAccountPage', true)) {
                $objectType = 'track';
                if ($this->permissionApi->hasPermission($this->getBundleName() . ':' . ucfirst($objectType) . ':', '::', ACCESS_READ)) {
                    $links[] = [
                        'url' => $this->router->generate('musoundmodule_' . strtolower($objectType) . '_view', ['own' => 1]),
                        'text' => $this->__('My tracks', 'musoundmodule'),
                        'icon' => 'list-alt'
                    ];
                }
            }

            if (true === $this->variableApi->get('MUSoundModule', 'linkOwnCollectionsOnAccountPage', true)) {
                $objectType = 'collection';
                if ($this->permissionApi->hasPermission($this->getBundleName() . ':' . ucfirst($objectType) . ':', '::', ACCESS_READ)) {
                    $links[] = [
                        'url' => $this->router->generate('musoundmodule_' . strtolower($objectType) . '_view', ['own' => 1]),
                        'text' => $this->__('My collections', 'musoundmodule'),
                        'icon' => 'list-alt'
                    ];
                }
            }

            if ($this->permissionApi->hasPermission($this->getBundleName() . '::', '::', ACCESS_ADMIN)) {
                $links[] = [
                    'url' => $this->router->generate('musoundmodule_collection_adminindex'),
                    'text' => $this->__('Sound Backend', 'musoundmodule'),
                    'icon' => 'wrench'
                ];
            }


            return $links;
        }

        $routeArea = LinkContainerInterface::TYPE_ADMIN == $type ? 'admin' : '';
        if (LinkContainerInterface::TYPE_ADMIN == $type) {
            if ($this->permissionApi->hasPermission($this->getBundleName() . '::', '::', ACCESS_READ)) {
                $links[] = [
                    'url' => $this->router->generate('musoundmodule_collection_index'),
                    'text' => $this->__('Frontend', 'musoundmodule'),
                    'title' => $this->__('Switch to user area.', 'musoundmodule'),
                    'icon' => 'home'
                ];
            }
        } else {
            if ($this->permissionApi->hasPermission($this->getBundleName() . '::', '::', ACCESS_ADMIN)) {
                $links[] = [
                    'url' => $this->router->generate('musoundmodule_collection_adminindex'),
                    'text' => $this->__('Backend', 'musoundmodule'),
                    'title' => $this->__('Switch to administration area.', 'musoundmodule'),
                    'icon' => 'wrench'
                ];
            }
        }
        
        if (in_array('album', $allowedObjectTypes)
            && $this->permissionApi->hasPermission($this->getBundleName() . ':Album:', '::', $permLevel)) {
            $links[] = [
                'url' => $this->router->generate('musoundmodule_album_' . $routeArea . 'view'),
                'text' => $this->__('Albums', 'musoundmodule'),
                'title' => $this->__('Album list', 'musoundmodule')
            ];
        }
        if (in_array('track', $allowedObjectTypes)
            && $this->permissionApi->hasPermission($this->getBundleName() . ':Track:', '::', $permLevel)) {
            $links[] = [
                'url' => $this->router->generate('musoundmodule_track_' . $routeArea . 'view'),
                'text' => $this->__('Tracks', 'musoundmodule'),
                'title' => $this->__('Track list', 'musoundmodule')
            ];
        }
        if (in_array('collection', $allowedObjectTypes)
            && $this->permissionApi->hasPermission($this->getBundleName() . ':Collection:', '::', $permLevel)) {
            $links[] = [
                'url' => $this->router->generate('musoundmodule_collection_' . $routeArea . 'view'),
                'text' => $this->__('Collections', 'musoundmodule'),
                'title' => $this->__('Collection list', 'musoundmodule')
            ];
        }
        if ($routeArea == 'admin' && $this->permissionApi->hasPermission($this->getBundleName() . '::', '::', ACCESS_ADMIN)) {
            $links[] = [
                'url' => $this->router->generate('musoundmodule_config_config'),
                'text' => $this->__('Configuration', 'musoundmodule'),
                'title' => $this->__('Manage settings for this application', 'musoundmodule'),
                'icon' => 'wrench'
            ];
        }

        return $links;
    }

    /**
     * Returns the name of the providing bundle.
     *
     * @return string The bundle name
     */
    public function getBundleName()
    {
        return 'MUSoundModule';
    }
}
